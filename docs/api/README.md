# API documentation

The public API will be versioned under `/api/v1/`. OpenAPI generation and product endpoints are scheduled for Phase 4.

Phase 1 exposes operational endpoints only:

- `GET /health/live`
- `GET /health/ready`
